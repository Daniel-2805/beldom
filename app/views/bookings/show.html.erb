<<<<<<< HEAD
<div class="container-show">
  <div class="d-flex justify-content-center">
    <div class="row align-center">

      <div class="col-6">
        <h1 class="text-center" style="font-family: san-serif">Tu servicio esta reservado</h1> 
        <p style="font-family: san-serif"><strong>Hora de entrada:</strong> <%= @booking.start_date.strftime("Dia: %m/%d/%Y hora: %I:%M%p") %></p>
        <p style="font-family: san-serif"><strong>Hora de salida</strong> <%= @booking.end_date.strftime("Dia: %m/%d/%Y hora: %I:%M%p") %></p>
        <p style="font-family: san-serif"><strong>Dirección</strong> <%= @booking.address_client %></p> 
        
        
        <%= link_to 'Cancelar reserva', booking_path(@booking), method: :delete, data: { confirm: "Estas seguro que quieres cancelar la reserva?" }, class: "btn btn-danger" %>
      

      </div>
      
      <div class="col-6">
        <h1 class="text-center" style="font-family: san-serif">Dejanos tus comentarios</h1> 
        <%= simple_form_for [@booking, @review] do |f| %>
        <%= f.input :content %>
        <%= f.input :rating %>
        <%= f.submit class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>	
  </div>
=======
<div class= "container ">       
  <h1 class= "h1 py-3 text-center">Reserva</h1>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class=" p-5 bg-white rounded mb-5 ">
       <p><strong>Hora de entrada:</strong> <%= @booking.start_date.strftime("Dia: %m/%d/%Y hora: %I:%M%p") %></p>
       <p><strong>Hora de salida</strong> <%= @booking.end_date.strftime("Dia: %m/%d/%Y hora: %I:%M%p") %></p>
       <p><strong>Dirección</strong> <%= @booking.address_client %></p>
       <%= link_to 'Cancelar reserva' , booking_path(@booking), method: :delete, data: { confirm: "Estas seguro que quieres cancelar la reserva?" }  %>
      </div>  
    </div>
  </div>   
<div class="container">
  <h2 class="h2 py-3 text-center">Dejanos tus comentario</h2>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class=" p-5 bg-white rounded mb-5 "> 
        <%= simple_form_for [@booking, @review] do |f| %>
          <%= f.input :content %>
          <%= f.input :rating %>
          <div class="form-actions">
          <%= f.button :submit, "Crear Reserva", class: "btn btn-primary btn-block" %>
          </div>
        <% end %>
      </div>
    </div>
  </div> 
</div>
>>>>>>> master
</div>